export default ({type, name, category, mechanism, family, description, image, thumbnail, bggId} = {}) => ({
    type,
    name,
    category,
    mechanism,
    family,
    description,
    image,
    thumbnail,
    bggId
})